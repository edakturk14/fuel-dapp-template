{
  "name": "@fuel-starter-kit/fuel",
  "version": "0.0.1",
  "scripts": {
    "dev": "yarn exec forc --version",
    "build": "cd contract && yarn exec forc build",
    "chain":"fuel-core run --db-type in-memory",
    "deploy:local": "cd contract && yarn exec forc deploy --node-url localhost:4000 --unsigned",
    "deploy:testnet": "cd contract && yarn exec forc deploy --node-url beta-3.fuel.network/graphql --gas-price 1 --random-salt"
  }
}
