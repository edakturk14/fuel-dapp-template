{
  "name": "@fuel-starter-kit/fuel",
  "version": "0.0.1",
  "scripts": {
    "dev": "yarn exec forc --version",
    "build": "cd contract && yarn exec forc build",
    "chain": "fuel-core run --ip 127.0.0.1 --port 4000 --chain ./chainConfig.json --db-path ./.fueldb"
  }
}
